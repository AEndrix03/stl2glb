version: "3.9"
services:
  stl2glb:
    build: .
    ports:
      - "9002:${STL2GLB_PORT}"
    env_file:
      - .env
    volumes:
      - ./:/project
    working_dir: /project/build
    command: ./stl2glb_exec
