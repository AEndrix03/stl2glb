# Dockerfile-tiny - Copia vcpkg dall'host prima del build
# PREREQUISITO: vcpkg deve essere presente in ./vcpkg/ nella directory di build

FROM alpine:3.19 AS builder

# Installa solo build tools essenziali
RUN apk add --no-cache \
    build-base \
    cmake \
    ninja \
    linux-headers

# Working directory
WORKDIR /build

# Copia vcpkg dal context (preparato dallo script)
COPY vcpkg/ /vcpkg/

# Verifica che vcpkg sia presente e funzionante
RUN if [ ! -f "/vcpkg/vcpkg" ]; then \
        echo "❌ ERRORE: vcpkg non trovato"; \
        echo "🔧 Esegui il build tramite: ./build-and-run.sh"; \
        exit 1; \
    fi && \
    if [ ! -d "/vcpkg/installed/x64-linux" ]; then \
        echo "❌ ERRORE: dipendenze vcpkg non installate per x64-linux"; \
        echo "🔧 Verifica l'installazione delle dipendenze"; \
        ls -la /vcpkg/installed/ || echo "Directory installed non trovata"; \
        exit 1; \
    fi && \
    echo "✅ vcpkg verificato correttamente" && \
    echo "📦 Dipendenze disponibili:" && \
    ls -la /vcpkg/installed/x64-linux/

# Copia sorgenti del progetto
COPY src/ ./src/
COPY CMakeLists.txt ./
COPY *.cpp *.h ./ 2>/dev/null || true

# Fix per Linux se necessario
RUN if [ -f "src/STLParser.cpp" ]; then \
    grep -q "#include <cmath>" src/STLParser.cpp || \
    sed -i '/#include <atomic>/a #include <cmath>' src/STLParser.cpp; \
    fi

# Build ottimizzata
RUN cmake -B build -S . \
    -DCMAKE_BUILD_TYPE=MinSizeRel \
    -DCMAKE_TOOLCHAIN_FILE=/vcpkg/scripts/buildsystems/vcpkg.cmake \
    -DVCPKG_TARGET_TRIPLET=x64-linux \
    -DVCPKG_INSTALLED_DIR=/vcpkg/installed \
    -GNinja && \
    cmake --build build --config MinSizeRel --parallel $(nproc) && \
    strip build/stl2glb_exec

# Stage 2: Runtime minimale
FROM alpine:3.19

# Installa librerie runtime e wget per health check
RUN apk add --no-cache \
    libstdc++ \
    libgcc \
    openssl \
    ca-certificates \
    wget && \
    adduser -D -u 1000 appuser

WORKDIR /app
USER appuser

# Copia eseguibile
COPY --from=builder --chown=appuser:appuser /build/build/stl2glb_exec /app/

EXPOSE 8080
ENTRYPOINT ["/app/stl2glb_exec"]